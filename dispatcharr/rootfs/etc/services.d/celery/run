#!/usr/bin/with-contenv bashio
cd /app
export PYTHONPATH="/app"
export PATH="/app/env/bin:$PATH"

# Load environment safely
set -a
. /app/.env
set +a

exec nice -n 5 /app/env/bin/python3 -m celery -A dispatcharr worker -l info --concurrency=1
