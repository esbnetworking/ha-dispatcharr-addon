#!/usr/bin/with-contenv bashio
cd /app
export PYTHONPATH="/app"
export $(grep -v '^#' /app/.env | xargs)
exec gunicorn --workers=4 --worker-class=gevent --timeout=300 --bind 0.0.0.0:8000 dispatcharr.wsgi:application
