#!/usr/bin/with-contenv bashio

# Load environment (critical)
set -a
. /app/.env
set +a

exec /app/env/bin/gunicorn dispatcharr.wsgi:application \
    --bind 0.0.0.0:8000 \
    --workers 3 \
    --timeout 300
