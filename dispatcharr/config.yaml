Replace with this:

name: "Dispatcharr"
slug: "dispatcharr"
description: "Dispatcharr IPTV management"
version: "1.0.0"

startup: services
boot: auto
init: false

host_network: true

arch:
  - aarch64
  - amd64

map:
  - addon_config:rw

options:
  web_port: 9191
  log_level: info

schema:
  web_port: port
  log_level: list(trace|debug|info|notice|warning|error|fatal)?
